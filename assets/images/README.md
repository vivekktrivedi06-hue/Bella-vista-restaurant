# Assets / Images

This folder is intentionally empty — the demo pulls images from **Unsplash** so the ZIP stays small and downloads fast.

Drop your own JPG / PNG / WebP images here and swap the `background-image` URLs in the HTML files if you'd like a fully offline build.
